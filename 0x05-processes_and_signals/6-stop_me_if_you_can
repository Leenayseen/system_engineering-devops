#!/usr/bin/env bash
# Find and silently terminate the "4-to_infinity_and_beyond" process if running

PID=$(pgrep -f "./4-to_infinity_and_beyond" 2>/dev/null)
[ -n "$PID" ] && kill -TERM "$PID" > /dev/null 2>&1
